interface:
  display_name: "Unidle"
  short_description: "Finish substantial work against runnable gates"
  default_prompt: "Use $unidle to complete this substantial task against explicit, verifiable gates."
policy:
  allow_implicit_invocation: true
